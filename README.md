# ZBRA Challenge
## Piero Acosta Desenzi

- To run the exercises, execute:
`python src/question1.py` and `python src/question2.py`. The results will we written on the stdout.
- To run the tests, execute:
`python -m pytest tests`.

AI was used solely to enhance documentation, but no actual code was written with AI.